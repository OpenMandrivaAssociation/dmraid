
Fixes string format build error.

Signed-off-by: Thomas Backlund <tmb@mandriva.org>

--- 1.0.0.rc16/lib/events/libdmraid-events-isw.c.orig	2009-09-16 11:55:23.000000000 +0300
+++ 1.0.0.rc16/lib/events/libdmraid-events-isw.c	2010-02-18 21:19:03.003560112 +0200
@@ -838,13 +838,13 @@ static void _log_either(enum log_type lo
 
 	sz = _log_all_devs(log_type, rs, NULL, 0);
 	if (!sz) {
-		syslog(LOG_ERR, msg[0]);
+		syslog(LOG_ERR, "%s", msg[0]);
 		return;
 	}
 
 	str = dm_malloc(++sz);
 	if (!str) {
-		syslog(LOG_ERR, msg[1]);
+		syslog(LOG_ERR, "%s", msg[1]);
 		return;
 	}
 
